# ===================================
#  Form Field Definitions
# ===================================

fields:
    # id:
    #     label: ID
    #     disabled: true

    name:
        label: Name
        type: text

    description:
        label: Description
        type: textarea
        size: large
    
    project_manager:
        label: Project Manager
        type: recordfinder
        list: ~/plugins/rainlab/user/models/user/columns.yaml
        prompt: Click the button to find a project manager

    customer:
        label: Customer
        type: recordfinder
        list: ~/plugins/rainlab/user/models/user/columns.yaml
        prompt: Click the button to find a customer

    due_date:
        label: Due Date
        type: datepicker

    is_done:
        label: Done
        type: switch

